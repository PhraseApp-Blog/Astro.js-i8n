---
import BaseLayout from '../layouts/base.astro';

const locale = Astro.currentLocale;;
if (Astro.url.pathname === '/') {
   return Astro.redirect(`/${locale}`);
}
---

<BaseLayout>
    No one should ever see this page.
</BaseLayout>